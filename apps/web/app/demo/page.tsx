import { AttackDemoPage } from "@/components/demo/attack-demo-page";

export const metadata = {
  title: "Attack demo",
  description:
    "Canonical obligation passport + Protocol A finance vs Protocol B blocked attack demo.",
};

export default function DemoPage() {
  return <AttackDemoPage />;
}
